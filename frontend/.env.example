# Supabase (本番接続時のみ。ローカル MVP では未設定でも localStorage で動作)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic Claude API (ClaudeApiImpl を有効にした時のみ必須)
ANTHROPIC_API_KEY=
